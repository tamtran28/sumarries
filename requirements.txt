# --- Giao diện người dùng ---
streamlit

# --- Xử lý file đầu vào ---
PyMuPDF     # Để đọc file .pdf (import fitz)
python-docx # Để đọc file .docx

# --- Xử lý tóm tắt văn bản ---
# Cần phiên bản < 4.0.0 để sử dụng "gensim.summarization"
gensim==3.8.3
